#include "main.h"
/**
 * _abs -  compute the absolute value of an integer
 * @i: integer to be computed
 * Return: The absolute value of i
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}

